#include <iostream>
#include <queue>
using std::cin;

int main()
{
    std::priority_queue<int> pq;
    int n, k;
    cin >> n >> k;
    int x;
    for(int i = 0; i < k; ++i){
        cin >> x;
        pq.push(x);
    }
    for(int i = k; i < n; ++i){
        cin >> x;
        if(x < pq.top()){
            pq.pop();
            pq.push(x);
        }
    }
    int s[k];
    for(int i = 0; i < k; ++i) {
        s[i] = pq.top();
        pq.pop();
    }
    for(int i = k-1; i >= 0; --i) {
        std::cout << s[i] << " ";
    }
    return 0;
}
